spring.application.name=postman

server.port=10201

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/postman?characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.hikari.maximum-pool-size=9
spring.datasource.hikari.minimum-idle=1
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create

spring.rabbitmq.port=5672
spring.rabbitmq.host=172.18.119.9
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

spring.cloud.stream.bindings.event.destination=event-stream
spring.cloud.stream.bindings.event.group=event-group
#通过该参数指定了分区键的表达式规则
spring.cloud.stream.bindings.event.producer.partitionKeyExpression=payload.id
#指定了消息分区的数量。
spring.cloud.stream.bindings.event.producer.partitionCount=2

event.exec.corn=*/1 * * * * ?

#eureka.client.serviceUrl.defaultZone=http://ygo:ygo@172.17.237.151:9000/eureka/
eureka.client.serviceUrl.defaultZone=http://127.0.0.1:9000/eureka/
eureka.instance.lease-renewal-interval-in-seconds=1
eureka.instance.lease-expiration-duration-in-seconds=2
eureka.client.registry-fetch-interval-seconds=5


############消息分区-消费者###############
# 对应 MQ 是 exchange  和消息发送者的 交换器是同一个
#spring.cloud.stream.bindings.inputProduct.destination=exchangeProduct
# 具体分组 对应 MQ 是 队列名称 并且持久化队列  inputProduct 自定义
#spring.cloud.stream.bindings.inputProduct.group=groupProduct999
#开启消费者分区功能
#spring.cloud.stream.bindings.inputProduct.consumer.partitioned=true
#指定了当前消费者的总实例数量
#spring.cloud.stream.instanceCount=2
#设置当前实例的索引号，从 0 开始
#spring.cloud.stream.instanceIndex=0
#######################################